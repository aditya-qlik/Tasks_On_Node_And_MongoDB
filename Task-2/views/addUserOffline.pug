include mixins/_offlineUser
<!DOCTYPE html>
html(lang="en")
  head
    style.
          .inner .form {
              display: flex;
              flex-direction: column;
              max-width: 100%;
          }
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Add User Offline
  body
    .inner
      h2  Add User To Table
      +offlineUsersForm()